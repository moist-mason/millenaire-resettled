Known issues
- Plaster walls don't connect (??)

To add immediately
- Stonecutter recipes for plastered mud palette
- Make plaster bucket a container item that can return an empty bucket when uesd as a crafting ingredient